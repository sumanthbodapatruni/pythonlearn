
# tuples can't change values like strings
s= {34,56,78,98,34,}
print(s)                # doesn't follow sequence and don't repeat the same values again
# s[0] indexing is not possible as they dont follow order
